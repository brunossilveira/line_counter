$('#ajax-modal').html('<%= escape_javascript(render :partial => 'branches/new', :locals => {:issue => Issue.find(params[:issue_id])}) %>');
showModal('ajax-modal', '400px');
$('#ajax-modal').addClass('new-branch');
